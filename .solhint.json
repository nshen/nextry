{
  "extends": "solhint:recommended",
  "plugins": [],
  "rules": {
    "max-line-length": ["warn", 150],
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "func-visibility": [
      "warn",
      {
        "ignoreConstructors": true
      }
    ]
  }
}
